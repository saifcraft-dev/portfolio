You are an expert full‑stack engineer and architect.

Assume the following:

I have already:
Planned or implemented a migration from:
Express 5 + Node.js + PostgreSQL (via Drizzle ORM)
To:
Firebase Authentication v9+
Firestore (NoSQL)
Cloudinary for media
React + Vite frontend deployed on Vercel
My frontend now uses:
React Query + Firebase/Firestore SDK for all data access (no new /api code being added).
I want to fully remove the old backend (server/ folder and PostgreSQL) once it is safe.
1. Current Situation (Post‑Migration State)
Previously:

Backend (server/):
Express 5 on Node.js
REST API under /api/*
Used PostgreSQL + Drizzle ORM
Frontend used:
TanStack React Query calling /api/... routes.
Now (target state):

Frontend:

React + Vite
React Query using Firestore SDK for:
Projects (portfolio items)
Services
Service orders/contact submissions
Firebase Auth for admin access and (optionally) user auth
Cloudinary for images
Backend:

Either:
No custom backend at all (pure client + Firebase/Firestore + Cloudinary), or
Only small serverless pieces (e.g., Firebase Cloud Functions / Vercel Functions) for secure tasks like signed Cloudinary uploads.
I want to finish the migration and clean up the project, including safely deleting the server/ folder.

2. What I Want From You
Give me a clear, step‑by‑step post‑migration guide that answers:

A. Verification Before Deleting server/
How do I systematically verify that:
No code in the frontend still calls /api/...?
All CRUD operations (projects, services, service orders) now go through Firestore?
All authentication and authorization flows work via Firebase Auth + Firestore rules?
Suggest:
Concrete checks/commands (e.g., search patterns like "/api/" in the codebase).
A small manual test plan:
Viewing portfolio/projects
Viewing services
Submitting a service order/contact request
Admin actions: create/update/delete projects/services
B. Cleaning Up the Codebase
Show how to:
Safely remove or archive the server/ directory:
Recommended Git workflow (e.g., branch or tag before deletion).
Update package.json scripts:
Remove any scripts like dev:server, dev:full, start:server, etc.
Keep only what is needed for Vite + frontend (e.g., dev, build, preview).
Explain:
How to adjust imports and TypeScript config if anything referenced shared types from server/ or shared/.
Whether to keep the shared/ directory or move relevant types into the frontend (src/types, for example).
C. Environment Variables & Configuration Cleanup
Help me:
Identify which .env / .env.local variables were only for the old backend/PostgreSQL, so I can remove them.
Keep and clearly document:
Firebase config variables
Cloudinary variables
Any other variables required for Vercel/Firebase/Cloudinary to work.
Show:
A good way to structure and name env vars for:
Local dev
Vercel production
Any cautions about exposed client‑side keys and how Firestore security rules / Cloudinary settings mitigate risk.
D. Final Project Structure & Dev Workflow
Propose an updated project structure for a frontend‑only app (plus optional serverless functions), for example:

client/ or root:
src/ (React app)
src/lib/firebase.ts
src/lib/firestore.ts
src/lib/cloudinary.ts (if needed)
src/hooks/ (React Query hooks)
Optional:
functions/ or api/ for Firebase/Cloud/Vercel functions (if used)
Update my development workflow:

What is the new recommended npm run dev command?
How do I run everything locally without the old Express server?
How should I run tests or linting in this new setup?
E. Production Readiness Checklist
Give me a final checklist to confirm I’m production‑ready after removing server/, including:

Firestore:
Proper indexes created for common queries (featured projects, filter by category, etc.)
Security rules enforced and tested (read/write rules for public vs admin)
Firebase Auth:
Admin user(s) set up
Route protection verified
Cloudinary:
Upload approach (unsigned preset vs signed) validated and secure
Image URLs/public IDs correctly stored in Firestore
Vercel:
Build logs clean
Environment variables set and tested
No failed network calls related to the old API
F. Optional: Rollback Strategy
Briefly describe a simple rollback strategy in case I discover a critical issue after removing server/, e.g.:

Keeping a Git tag or branch with the old server/ code.
How I could temporarily re‑enable the old backend (conceptually) if needed.
3. Output Format
Organize your answer into these sections:

Pre‑Deletion Verification Steps
Codebase & Script Cleanup
Environment & Config Cleanup
Updated Project Structure & Dev Workflow
Production Readiness Checklist
Simple Rollback Strategy
Keep the answer practical and concise, with concrete examples (file names, command examples, search patterns).